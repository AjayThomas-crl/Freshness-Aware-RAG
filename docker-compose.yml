services:
  app:
    build: .
    container_name: freshness-rag
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////app/data/rag.db
      - CHROMA_DIR=/app/data/chroma
    volumes:
      - ./data:/app/data
    restart: unless-stopped
